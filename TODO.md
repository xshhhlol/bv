# 待修复问题

- [ ] 视频切换：在 `playNewVideo()` 中取消旧视频的自动下一集倒计时，并清空旧提示，避免旧倒计时打断新视频。
- [ ] 编码选择：将新计算的目标清晰度传给 `getTargetVideoCodec()`，避免读取上一视频清晰度并回退到错误视频流。
- [ ] 动态关注缓存：关注／取关后刷新侧栏；切换账号时清空或按账号隔离关注列表、动态列表及 UP 投稿缓存。
